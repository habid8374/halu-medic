# API del backend — apunta al tenant demo (schema del consultorio).
# Los datos clínicos viven en el subdominio del consultorio (demo.localhost).
NEXT_PUBLIC_API_URL=http://demo.localhost:8000
NEXT_PUBLIC_APP_NAME=Halu Medic
NEXT_PUBLIC_APP_VERSION=1.0.0
